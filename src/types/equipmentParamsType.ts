export type EquipmentParams = {
    search?: string;
    minRating?: string;
    maxRating?: string;
    minPrice?: string;
    maxPrice?: string;
    isAvailable?: boolean;
    orderBy?: string;
    sort?: string;
}